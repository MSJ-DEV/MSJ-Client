import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const foundUs = () => {
    return (
        <View>
            <Text> found us page heloo</Text>
        </View>
    )
}

export default foundUs

const styles = StyleSheet.create({})
